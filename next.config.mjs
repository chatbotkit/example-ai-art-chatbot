/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "cdn.mediatheque.epmoo.fr",
      "sep.turbifycdn.com",
    ],
  },
};

export default nextConfig;
